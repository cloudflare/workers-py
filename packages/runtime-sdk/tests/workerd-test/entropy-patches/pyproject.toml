[project]
name = "test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "pytest",
    "pytest-asyncio<1.2.0",
    "numpy",
    "pydantic",
    "requests",
    "urllib3",
    "tiktoken",
    "cryptography",
    "jiter",
    "aiohttp",
    "langsmith",
    # TODO(fixme): Cannot use random.getrandbits() outside of request context
    # "langchain-openai",
    # "openai",
    "litestar",
    "opentelemetry-sdk",
]
